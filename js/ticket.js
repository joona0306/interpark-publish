window.addEventListener("load", function () {
  console.log("티켓코딩");

  // 1. 외부 데이터를 불러온다.
  const xhr = new XMLHttpRequest();

  // 2. html 태그를 백틱을 이용해서 만든다.

  // 3. swipre 태그에 백틱을 배치한다.

  // 4. swiper 작동시킨다.
});
